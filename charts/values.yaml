Deployment:
replicaCount: 2
namespace: dev
name: my-app
image:
  repository: python
  tag: "3.11-slim"
  pullPolicy: IfNotPresent
autoscaling:
  enabled: false

service:
  namespace: dev
  type: ClusterIP
  port: 8080
  targetPort: 8080
  name: my-service
